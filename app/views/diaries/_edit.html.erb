<% @diary = Diary.new %>
<%= simple_form_for(@diary) do |f| %>

  <div class="field" >
    <h3>Заголовок</h3>
    <%= f.input :title, label: false, class: "form-control", placeholder: "Заголовок" %>
  </div>

  <div class="field">
    <h3>Заметка</h3>
    <%= f.input :content, as: :text, label: false, class: "form-control", placeholder: "Заметка..." %>
  </div>
  <br />
  


  <!-- ДОБАВИТЬ КНОПКИ -->
  
<% end %>